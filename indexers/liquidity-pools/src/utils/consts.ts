/**
 * Shared Hydration chain constants.
 *
 * Previously some of these came from `@galacticcouncil/sdk`, which has been
 * removed upstream (the source package is gone from galacticcouncil/sdk;
 * only the frozen npm 10.x lingers). Defined locally so we no longer depend
 * on the deprecated package for them.
 */

/** SS58 address prefix for the Hydration parachain. */
export const HYDRADX_SS58_PREFIX = 63;
